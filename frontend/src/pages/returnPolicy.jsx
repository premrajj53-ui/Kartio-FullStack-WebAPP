import React from 'react';

const ReturnPolicy = () => {
  return (
    <main className="info-page">
      <section className="info-panel">
        <span className="info-label">Customer Support</span>
        <h1>Return Policy</h1>
        <p>
          You can request a return if the product is damaged, incorrect, or not as described.
          Items should be unused and kept with their original packaging where possible.
        </p>
        <p>
          Return requests should be made soon after delivery. Once the returned item is checked,
          eligible refunds or replacements will be processed through the original order details.
        </p>
      </section>
    </main>
  );
};

export default ReturnPolicy;
